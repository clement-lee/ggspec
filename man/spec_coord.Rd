% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/spec_coord.R
\name{spec_coord}
\alias{spec_coord}
\title{Extract coordinate system specification as a tidy data frame}
\usage{
spec_coord(p)
}
\arguments{
\item{p}{A ggplot object.}
}
\value{
A \code{\link[tibble:tibble]{tibble::tibble()}} with one row and columns:
\describe{
\item{\code{coord_type}}{Short name of the coordinate system, e.g.
\code{"cartesian"}, \code{"flip"}, \code{"polar"}, \code{"sf"}.}
\item{\code{xlim}}{List-column: numeric vector of x limits, or \code{NULL} if not
set.}
\item{\code{ylim}}{List-column: numeric vector of y limits, or \code{NULL} if not
set.}
\item{\code{expand}}{Logical: whether axes are expanded beyond the data
range.}
\item{\code{clip}}{Whether clipping is applied: \code{"on"}, \code{"off"}, or
\code{NA}.}
}
}
\description{
Returns a single-row data frame describing the plot's coordinate system.
}
\examples{
p1 <- ggplot2::ggplot(ggplot2::mpg, ggplot2::aes(displ, hwy)) +
  ggplot2::geom_point()
spec_coord(p1)

p2 <- p1 + ggplot2::coord_flip()
spec_coord(p2)

p3 <- p1 + ggplot2::coord_polar()
spec_coord(p3)
}
