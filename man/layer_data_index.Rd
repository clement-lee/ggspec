% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils.R
\name{layer_data_index}
\alias{layer_data_index}
\title{Find which layer of a plot uses a given data frame}
\usage{
layer_data_index(p, data)
}
\arguments{
\item{p}{A ggplot object.}

\item{data}{A data frame to search for.}
}
\value{
An integer scalar.
}
\description{
Returns an integer indicating where \code{data} appears in the plot:
\itemize{
\item \code{0L} — the plot-level (\code{p$data}) data
\item \code{i} (positive integer) — \code{p$layers[[i]]$data}
\item \code{NA_integer_} — not found
}
}
\examples{
drv_summary <- dplyr::group_by(ggplot2::mpg, drv) |>
  dplyr::summarise(mean_displ = mean(displ))
p <- ggplot2::ggplot() +
  ggplot2::geom_jitter(data = ggplot2::mpg,
                       ggplot2::aes(x = drv, y = displ)) +
  ggplot2::geom_point(data = drv_summary,
                      ggplot2::aes(x = drv, y = mean_displ))
layer_data_index(p, drv_summary)
}
