% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/spec_facets.R
\name{spec_facets}
\alias{spec_facets}
\title{Extract facet specification as a tidy data frame}
\usage{
spec_facets(p)
}
\arguments{
\item{p}{A ggplot object.}
}
\value{
A \code{\link[tibble:tibble]{tibble::tibble()}} with one row and columns:
\describe{
\item{\code{facet_type}}{One of \code{"null"}, \code{"wrap"}, or \code{"grid"}.}
\item{\code{rows}}{For \code{facet_grid()}: character representation of the row
faceting variable(s). \code{NA} otherwise.}
\item{\code{cols}}{Character representation of the column faceting
variable(s). For \code{facet_wrap()} this holds the wrap variable(s).}
\item{\code{scales}}{Scale freedom: \code{"fixed"}, \code{"free"}, \code{"free_x"}, or
\code{"free_y"}.}
\item{\code{space}}{Space freedom (grid only): \code{"fixed"}, \code{"free"}, etc.}
\item{\code{labeller}}{Labeller as a string, e.g. \code{"label_value"}.}
}
}
\description{
Returns a single-row data frame describing the plot's faceting. When no
faceting is applied, \code{facet_type} is \code{"null"} and all other columns are
\code{NA}.
}
\examples{
p1 <- ggplot2::ggplot(ggplot2::mpg, ggplot2::aes(displ, hwy)) +
  ggplot2::geom_point() +
  ggplot2::facet_wrap(~class)
spec_facets(p1)

p2 <- ggplot2::ggplot(ggplot2::mpg, ggplot2::aes(displ, hwy)) +
  ggplot2::geom_point() +
  ggplot2::facet_grid(drv ~ cyl)
spec_facets(p2)
}
