% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/check.R
\name{check_plot}
\alias{check_plot}
\title{Framework-agnostic plot check}
\usage{
check_plot(
  p,
  expected,
  check = NULL,
  mode = NULL,
  fail_fn = stop,
  pass_fn = invisible,
  ...
)
}
\arguments{
\item{p}{The observed ggplot object (e.g., the student's plot).}

\item{expected}{The reference ggplot object.}

\item{check}{Character vector of checks to run; passed to \code{\link[=equiv_plot]{equiv_plot()}} or
\code{\link[=compare_plots]{compare_plots()}}.}

\item{mode}{Character scalar: comparison mode. If \code{NULL} (default),
\code{\link[=equiv_plot]{equiv_plot()}} is used (direct comparison). If \code{"strict"} or
\code{"structural"}, \code{\link[=compare_plots]{compare_plots()}} is used with \code{\link[=canon]{canon()}}. If \code{"visual"}
or \code{"conceptual"}, \code{\link[=compare_plots]{compare_plots()}} dispatches to \code{\link[=compare_visual]{compare_visual()}} or
\code{\link[=compare_conceptual]{compare_conceptual()}} respectively.}

\item{fail_fn}{A function called with the failure message string when the
check does not pass. Defaults to \code{\link[=stop]{stop()}}. Useful alternatives:
\code{gradethis::fail}, \code{warning}, \code{message}, or a custom function.}

\item{pass_fn}{A function called with the success message string when all
checks pass. Defaults to \code{\link[=invisible]{invisible()}}.}

\item{...}{Additional arguments passed to \code{\link[=equiv_plot]{equiv_plot()}} or
\code{\link[=compare_plots]{compare_plots()}}.}
}
\value{
The \code{ggspec_result} invisibly. Side effects (calling \code{fail_fn} or
\code{pass_fn}) are the primary interface.
}
\description{
Compares a student/observed plot against a reference plot and calls
\code{fail_fn} if any check fails. By default \code{fail_fn = stop}, so the function
works in any R context. Swap in \code{gradethis::fail()} for learnr grading or a
\code{testthat} expectation for unit testing.
}
\examples{
ref <- ggplot2::ggplot(ggplot2::mpg, ggplot2::aes(displ, hwy)) +
  ggplot2::geom_point()

obs_correct <- ggplot2::ggplot(ggplot2::mpg, ggplot2::aes(displ, hwy)) +
  ggplot2::geom_point()

obs_wrong <- ggplot2::ggplot(ggplot2::mpg, ggplot2::aes(displ, hwy)) +
  ggplot2::geom_line()

# Passes silently (direct comparison)
check_plot(obs_correct, ref, check = "layers")

# Passes silently (structural mode: layer order ignored)
obs_reordered <- ggplot2::ggplot(ggplot2::mpg, ggplot2::aes(displ, hwy)) +
  ggplot2::geom_point()
check_plot(obs_reordered, ref, check = "layers", mode = "structural")

# Calls stop() with a message
tryCatch(
  check_plot(obs_wrong, ref, check = "layers"),
  error = function(e) message("Caught: ", conditionMessage(e))
)
}
