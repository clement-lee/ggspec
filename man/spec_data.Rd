% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/spec_data.R
\name{spec_data}
\alias{spec_data}
\title{Extract the dataset table for a ggplot object}
\usage{
spec_data(p)
}
\arguments{
\item{p}{A ggplot object.}
}
\value{
A \code{\link[tibble:tibble]{tibble::tibble()}} with columns:
\describe{
\item{\code{data_id}}{Integer: sequential identifier starting at 1.}
\item{\code{label}}{Character: \code{"hash_<first8chars>"} of the data frame's
hash.  The original symbol or expression is unavailable after
evaluation; the hash is unique and consistent across calls.}
}
}
\description{
Returns one row per unique data frame used in the plot — either at the
global level (\code{ggplot(data, ...)}) or attached to individual layers
(\code{geom_*(data = ...)}). Datasets are deduplicated by their \code{\link[rlang:hash]{rlang::hash()}}.
}
\examples{
p <- ggplot2::ggplot(ggplot2::mpg, ggplot2::aes(displ, hwy)) +
  ggplot2::geom_point()
spec_data(p)
}
