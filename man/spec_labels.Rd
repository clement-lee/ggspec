% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/spec_labels.R
\name{spec_labels}
\alias{spec_labels}
\title{Extract plot labels as a tidy data frame}
\usage{
spec_labels(p)
}
\arguments{
\item{p}{A ggplot object.}
}
\value{
A \code{\link[tibble:tibble]{tibble::tibble()}} with one row per label and columns:
\describe{
\item{\code{aesthetic}}{The name of the label slot, e.g. \code{"title"}, \code{"x"},
\code{"colour"}.}
\item{\code{label}}{The label string. \code{NA} if the slot is \code{NULL} or a
\code{waiver()}.}
}
}
\description{
Returns one row per label currently set on the plot (title, subtitle,
caption, tag, and any aesthetic labels such as x, y, colour, fill, etc.).
}
\examples{
p <- ggplot2::ggplot(ggplot2::mpg, ggplot2::aes(displ, hwy, colour = class)) +
  ggplot2::geom_point() +
  ggplot2::labs(title = "Engine vs highway MPG", x = "Displacement (L)")
spec_labels(p)
}
