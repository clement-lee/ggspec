% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/canon.R
\name{canon}
\alias{canon}
\title{Canonicalise a ggplot specification}
\usage{
canon(x, mode = "structural")
}
\arguments{
\item{x}{A ggplot object, a \code{\link[=spec_plot]{spec_plot()}} tibble, or a \code{ggspec_canon}
object. If a \code{ggspec_canon} is supplied, its \verb{$spec} is re-canonicalised
(useful for chaining or verifying idempotency).}

\item{mode}{Character scalar controlling which normalisation rules are
applied. \code{canon()} is a \strong{term rewriting system (TRS)} that computes
a structural normal form: two plots are structurally equivalent iff they
have the same normal form under these rules.
\describe{
\item{\code{"strict"}}{Minimal normalisation: ensure all values are plain
character strings (no raw quosures) and list-column NULLs are
standardised. Preserves data/mapping placement and layer order.}
\item{\code{"structural"} (default)}{Includes strict, plus: fold global
data/mapping into each non-zero layer so that placement differences
(global vs per-layer) become transparent; sort layers into a
canonical order by \verb{(geom, stat)}; normalise geom/stat
representation (\code{geom_col} -> \code{geom_bar(stat="identity")}).}
}
Visual and conceptual comparison (which require rendering via
\code{\link[ggplot2:ggplot_build]{ggplot2::ggplot_build()}}) are handled by \code{\link[=compare_visual]{compare_visual()}} and
\code{\link[=compare_conceptual]{compare_conceptual()}} respectively, not by \code{canon()}.}
}
\value{
An object of class \code{ggspec_canon}, a named list with components:
\describe{
\item{\code{spec}}{A \code{\link[=spec_plot]{spec_plot()}} tibble after canonicalisation.}
\item{\code{changes}}{A tibble with columns \code{rule} (chr), \code{dimension} (chr),
\code{layer} (int or \code{NA}), \code{from} (chr), \code{to} (chr).  Zero rows means
the input was already in canonical form.}
\item{\code{mode}}{The mode string used.}
\item{\code{original}}{The \code{\link[=spec_plot]{spec_plot()}} tibble before canonicalisation.}
}
}
\description{
Normalises a ggplot specification (extracted via \code{\link[=spec_plot]{spec_plot()}}) into a
standard form, resolving multiple representations of the same intent.
The result is an \code{ggspec_canon} object that carries the canonicalised
specification, a log of every change made, and the original specification
for comparison.
}
\details{
\code{canon()} is \strong{idempotent}: \code{canon(canon(x))} produces the same
specification as \code{canon(x)}, and the second call records zero changes.

\code{canon()} is \strong{transparent}: \code{x$changes} is a regular tibble listing
every normalisation applied, with columns \code{rule}, \code{dimension},
\code{layer}, \code{from}, and \code{to}.
}
\examples{
# Layer ordering is canonical in structural mode
p1 <- ggplot2::ggplot(ggplot2::mpg, ggplot2::aes(displ, hwy)) +
  ggplot2::geom_smooth() +
  ggplot2::geom_point()
c1 <- canon(p1)
c1$changes        # records the layer reordering
c1$spec$geom      # layer 0 NA, then alphabetical: "point", "smooth"

# Idempotency
c2 <- canon(c1)
nrow(c2$changes)  # 0 — already canonical
}
