% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/spec_scales.R
\name{spec_scales}
\alias{spec_scales}
\title{Extract scale specifications as a tidy data frame}
\usage{
spec_scales(p)
}
\arguments{
\item{p}{A ggplot object.}
}
\value{
A \code{\link[tibble:tibble]{tibble::tibble()}} with one row per scale and columns:
\describe{
\item{\code{aesthetic}}{The aesthetic the scale controls, e.g. \code{"x"},
\code{"colour"}.}
\item{\code{scale_class}}{Full S3 class string of the scale object.}
\item{\code{scale_type}}{Short type label, e.g. \code{"continuous"},
\code{"discrete"}, \code{"binned"}, \code{"manual"}, \code{"identity"}.}
\item{\code{name}}{Scale name / axis label (\code{waiver()} reported as \code{NA}).}
\item{\code{transform}}{Name of the transformation applied (continuous scales
only; \code{NA} otherwise).}
\item{\code{guide}}{Guide type as a string, e.g. \code{"legend"}, \code{"colourbar"},
\code{"none"}.}
}
}
\description{
Returns one row per explicitly added scale. Scales that were never added
by the user (i.e. default scales inferred at render time) are not included
because they do not exist in the ggplot object before \code{\link[ggplot2:ggplot_build]{ggplot2::ggplot_build()}} is
called; use \code{\link[=spec_plot]{spec_plot()}} to access built scales.
}
\examples{
p <- ggplot2::ggplot(ggplot2::mpg, ggplot2::aes(displ, hwy, colour = class)) +
  ggplot2::geom_point() +
  ggplot2::scale_colour_brewer(palette = "Set1") +
  ggplot2::scale_x_log10()
spec_scales(p)
}
