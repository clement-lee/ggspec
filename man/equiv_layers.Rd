% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/equiv.R
\name{equiv_layers}
\alias{equiv_layers}
\title{Compare layer structure of two ggplot objects}
\usage{
equiv_layers(p1, p2, exact = FALSE, check_order = FALSE)
}
\arguments{
\item{p1}{Reference ggplot or \code{ggspec_canon} object.}

\item{p2}{Observed ggplot or \code{ggspec_canon} object.}

\item{exact}{Logical. If \code{TRUE}, the number and order of layers must match
exactly. If \code{FALSE} (default), \code{p2} must contain at least the layers
present in \code{p1} (as a subset, ignoring order).}

\item{check_order}{Logical. If \code{TRUE} and \code{exact = FALSE}, layer order must
also match. Ignored when \code{exact = TRUE}.}
}
\value{
A \code{ggspec_result}.
}
\description{
Compare layer structure of two ggplot objects
}
\examples{
p1 <- ggplot2::ggplot(ggplot2::mpg, ggplot2::aes(displ, hwy)) +
  ggplot2::geom_point()
p2 <- p1 + ggplot2::geom_smooth()
equiv_layers(p1, p2)           # passes (p1's layers are a subset of p2)
equiv_layers(p1, p2, exact = TRUE)  # fails (p2 has more layers)
}
