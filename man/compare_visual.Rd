% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/visual.R
\name{compare_visual}
\alias{compare_visual}
\title{Compare two ggplot objects for visual equivalence}
\usage{
compare_visual(p1, p2, check = c("rendered", "labels", "facets", "coord"))
}
\arguments{
\item{p1}{The reference ggplot object.}

\item{p2}{The observed ggplot object.}

\item{check}{Character vector of checks to run. Options:
\code{"rendered"} (compare built layer data), \code{"labels"} (compare effective
axis/legend labels after absorbing scale names), \code{"facets"} (compare
facet configuration), \code{"coord"} (compare coordinate system after
coord_flip normalisation).}
}
\value{
A \code{ggspec_compare} object.
}
\description{
\code{compare_visual()} checks whether two plots produce identical rendered
output. It uses \code{\link[ggplot2:ggplot_build]{ggplot2::ggplot_build()}} for data comparison rather than spec
inspection, making it capable of detecting equivalences that structural
comparison cannot (e.g. pre-computed vs stat-based geoms, \code{coord_flip()}
with swapped aesthetics).
}
\details{
Called internally by \code{\link[=compare_plots]{compare_plots()}} when \code{mode = "visual"}.
}
\examples{
library(ggplot2); library(dplyr)
p1 <- ggplot(mpg, aes(x = class)) + geom_bar()
p2 <- mpg |> count(class) |> ggplot(aes(x = class, y = n)) + geom_col()
compare_visual(p1, p2)
}
