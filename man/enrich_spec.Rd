% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/build_spec.R
\name{enrich_spec}
\alias{enrich_spec}
\title{Enrich a plot's layer specification with build-derived default information}
\usage{
enrich_spec(p)
}
\arguments{
\item{p}{A ggplot object.}
}
\value{
A \code{\link[tibble:tibble]{tibble::tibble()}} extending \code{\link[=spec_layers]{spec_layers()}} with two additional
list-columns:
\describe{
\item{\code{params_tbl}}{One row per non-aesthetic parameter stored in the
layer.  Columns: \code{param} (chr), \code{value} (list), \code{explicit} (lgl),
\code{source} (chr — \code{"geom"}, \code{"stat"}, or \code{"aes"}).
\code{explicit = TRUE} means the value differs from the ggplot2 default (or
is a fixed aesthetic constant); \code{explicit = FALSE} means ggplot2 would
have used the same value without any user input.}
\item{\code{built_aes}}{One row per aesthetic resolved during build.  Columns:
\code{aesthetic} (chr), \code{value} (list), \code{explicit} (lgl).
\code{explicit = TRUE} means the aesthetic was mapped or set as a constant
by the user; \code{explicit = FALSE} means ggplot2 applied a default
(e.g. \code{colour = "black"} for \code{geom_point()} when no colour mapping is
present).}
}
}
\description{
Calls \code{\link[ggplot2:ggplot_build]{ggplot2::ggplot_build()}} and compares the result against the declared
spec to classify each parameter and aesthetic as \strong{explicit} (set by the
user) or \strong{default} (filled in by ggplot2).
}
\details{
The three-way classification follows directly from the spec/build comparison:
\itemize{
\item \strong{explicit} — quantity appears in both spec and build (user-specified).
\item \strong{default} — quantity is in build but absent from spec, or its value
matches the ggplot2 default (ggplot2 filled it in).
\item \strong{transformed} — quantity is in spec but its build value differs
(e.g. a colour name resolved to a hex code by a scale); detectable by
comparing \code{built_aes$value} with the original aesthetic variable.
}

\strong{How explicit detection works}:
ggplot2 stores all parameters — both user-supplied and defaulted — in
\code{layer$geom_params} / \code{layer$stat_params}.  To distinguish them, \code{enrich_spec()}
constructs a reference layer (via \verb{geom_*()}  with no arguments) and compares
each stored value against the reference default.  A value that differs from
its default is classed as explicit; one that matches is classed as default.
The one edge case this cannot detect is a user explicitly setting a param to
its own default value (e.g. \code{geom_smooth(se = TRUE)} when \code{TRUE} is the
default) — such cases are conservatively reported as default.
Aesthetic constants set via fixed values (e.g. \code{geom_point(colour = "red")})
are always reported as explicit regardless of value.
}
\examples{
p <- ggplot2::ggplot(ggplot2::mpg, ggplot2::aes(displ, hwy)) +
  ggplot2::geom_point(ggplot2::aes(colour = class))
es <- enrich_spec(p)
# Which params are explicit vs default for layer 1?
es$params_tbl[[1]]
# Which aesthetics are explicit vs default for layer 1?
es$built_aes[[1]]
}
\seealso{
\code{\link[=spec_layers]{spec_layers()}}, \code{\link[=spec_aes]{spec_aes()}}
}
