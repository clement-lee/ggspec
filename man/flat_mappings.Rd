% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils.R
\name{flat_mappings}
\alias{flat_mappings}
\title{Extract all aesthetic mappings as a flat data frame}
\usage{
flat_mappings(p)
}
\arguments{
\item{p}{A ggplot object.}
}
\value{
A \code{data.frame} with columns \code{aes} (character) and \code{var} (character).
Rows where the variable is \code{NA} (e.g. aesthetics set to constants) are
excluded.
}
\description{
Returns one row per unique (aesthetic, variable) pair found anywhere in the
plot — across all layers and the global mapping — after resolving
inheritance. This is a simpler alternative to \code{\link[=spec_aes]{spec_aes()}} when you only
need a flat lookup table.
}
\examples{
p <- ggplot2::ggplot(ggplot2::mpg, ggplot2::aes(displ, hwy)) +
  ggplot2::geom_point(ggplot2::aes(colour = class))
flat_mappings(p)
}
