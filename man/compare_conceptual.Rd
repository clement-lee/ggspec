% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/conceptual.R
\name{compare_conceptual}
\alias{compare_conceptual}
\title{Compare two ggplot objects for conceptual similarity}
\usage{
compare_conceptual(p1, p2)
}
\arguments{
\item{p1}{The reference ggplot object.}

\item{p2}{The observed ggplot object.}
}
\value{
A \code{ggspec_compare} object.
}
\description{
\code{compare_conceptual()} checks whether two plots communicate the same
information using potentially different visual encodings.  It runs a
sequence of bespoke detectors; if any detector fires, the plots are
declared conceptually similar.  If none fires, it falls back to
\code{\link[=compare_visual]{compare_visual()}}.
}
\details{
Conceptual similarity is always qualified by a WHEN condition, which is
described in the \verb{$message} field of the returned object.

Called internally by \code{\link[=compare_plots]{compare_plots()}} when \code{mode = "conceptual"}.
}
\examples{
library(ggplot2)
p_box  <- ggplot(mpg, aes(x = class, y = hwy)) + geom_boxplot()
p_viol <- ggplot(mpg, aes(x = class, y = hwy)) + geom_violin()
compare_conceptual(p_box, p_viol)   # TRUE
}
